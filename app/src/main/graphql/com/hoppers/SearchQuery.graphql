query SearchThings($queryString: String!, $type: SearchType!) {
    search(query: $queryString, type: $type, first: 10) {
        repositoryCount
        nodes {
            ... Repos
            ... UserFields
        }
    }
}
