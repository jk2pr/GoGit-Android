fragment  Repos on Repository {
    repoName:name
    defaultBranchRef {
        name
    }
    owner {
        login
        avatarUrl
    }
    description
    url
    primaryLanguage {
        name
        color
    }
    stargazerCount
    forkCount
    createdAt
    updatedAt
}